#include <bits/stdc++.h>
using namespace std;

int characterReplacement(string s, int k) {
  int n = s.length();
  int i = 0, j = 0, maxLen = 0, cnt = 0;

  vector<int> mpp(255, 0);

  while (j < n) {
    if (s[j] != s[i] && cnt == k) {
      while ()
    }
  }
}