#include <bits/stdc++.h>
using namespace std;

int func(vector<int> nums, int k) {
  int n = nums.size();

  int i = 0, j = 0, cnt = 0;
  int t = 0;

  while (j < n) {
    while (i <= j && t > k) {
    }
  }
}

int numberOfSubarrays(vector<int> nums, int k) { int n = nums.size(); }